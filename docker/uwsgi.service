#!/bin/bash
! $([[  "$CLA_ENV" == "prod" || "$CLA_ENV" == "staging" ]]) ||  grep sentry /etc/hosts > /dev/null 2>&1 || echo "$SENTRY_IPADRESS $SENTRY_HOSTNAME" >> /etc/hosts
cd /home/app/django

python manage.py collectstatic --noinput

exec /usr/local/bin/uwsgi --ini /etc/wsgi/conf.d/cla_frontend.ini
