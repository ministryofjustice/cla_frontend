apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
 name: {{ include "cla-frontend.fullname" . }}-service
spec:
 selector:
   matchLabels:
     app.kubernetes.io/instance: {{ .Release.Name }}
 endpoints:
 - port: metrics
   interval: 15s
