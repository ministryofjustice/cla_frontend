name: Lint ✏️
run-name: Lint checks ✏️

on:
  pull_request:
    branches: ["**"]

permissions: {}

env:
  environment: base
  timezone: ${{ vars.TIMEZONE }}

jobs:
  setup:
    name: Setup 🔧
    runs-on: ["ubuntu-latest"]
    outputs:
      environment: ${{ env.environment }}
      timezone: ${{ env.timezone }}
    steps:
      - name: Environment 🧪
        run: echo "Environment set to ${{ env.environment }}"

      - name: Timezone 🌐
        run: echo "Timezone set to ${{ env.timezone }}"

  python:
    permissions:
      contents: read

    name: 🐍 Python
    runs-on: ["ubuntu-latest"]
    container:
      image: python:2.7.18-slim
    needs: [setup]
    environment:
      name: ${{ needs.setup.outputs.environment }}

    steps:
      - name: Repository
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1

      - name: PIP
        shell: bash
        working-directory: ./
        run: |
          curl -sSL https://bootstrap.pypa.io/pip/2.7/get-pip.py -o get-pip.py
          echo "14ef322f183792f3984365b263b6a983b6329c36c64639ed811804c8f0003058  get-pip.py" | sha256sum -c -
          python get-pip.py --user

      - name: Requirements
        shell: bash
        working-directory: ./
        run: python -m pip install -r requirements/generated/requirements-lint.txt

      - name: Lint
        shell: bash
        working-directory: ./
        run: python -m flake8 --exit-zero cla_frontend
